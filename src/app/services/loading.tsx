import { EosIconsBubbleLoading } from "@/components/Loading";

export default function Loading() {
  return (
    <div className="flex items-center justify-center h-screen">
      <EosIconsBubbleLoading />
    </div>
  );
}
